def solution(n):
    temp = list(map(int,str(n)))
    temp.sort(reverse=True)
    return int(''.join(map(str,temp))
    
def solution(n):
  temp = list(str(n))
  temp.sort(reverse = True)
  return int("".join(temp))
               
# testcase 2,3, 11 오류 
